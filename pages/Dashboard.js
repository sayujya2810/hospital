import SideNavbar from "./SideNavbar";
import {auth} from "../utils/firebase";

const Dashboard = () => {
    return (
        <div>
            <SideNavbar />
        </div>
    );
}

export default Dashboard